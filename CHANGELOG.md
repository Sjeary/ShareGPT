# 更新日志

本项目的所有重要变更都会记录在本文件中。

格式基于 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.10] - 2026-09-13

> **更可靠的聊天、独立资料与连接恢复**。本次重点改善日常使用中的消息定位、翻译停止、环境重建和多设备同步，并让个人与团队资料的归属更清楚。

### 聊天与翻译

- 未读消息只在最早未读的位置显示一条分隔线，读到最新后消失；成员上下线不再产生重复横线。
- 已读到底的会话再次打开时保持在最新消息；正在翻看历史时则保留原位置，即使滚动后立即切换会话或离开聊天页也能恢复。
- 改善公共房间消息在历史加载、实时接收及重新连接后的排序和定位。
- 双表情组合图片加载失败时保留原表情，并可点击重试；网络恢复后无需重启即可继续使用。快速切换不同组合时不再混用上一组图片状态。
- 停止阅读翻译或发送预览时，不会因请求恰好完成而误触发第二次翻译；关闭翻译栏也会结束进行中的任务。

### 登录与 AI 网页

- 团队连接中断时，可从窗口右上角“重新登录”恢复连接，不必先退出账号。需要更新密码时可直接输入，网络失败后保留已填内容。
- 改善连续点击登录、切换账号和旧请求结束时的提示，避免过期操作干扰当前登录。
- 修复 Windows 删除高级环境后无法再次创建的问题；清理失败可重试，重新创建的环境不会被旧清理任务误删。
- 管理员确认不同入口指向同一套协作服务后，可接续原账号在本机保存的设置和 AI 网页环境，无需仅因入口地址变化而重新建立资料。

### 资料与工作区

- 个人工作区和不同团队账号分别保存日历、待办、笔记与专注记录，团队聊天历史按账号单独保存；切换回来或重启后继续使用对应资料。
- 在“账户 → 本机资料”中按类别接续旧资料，确认归属后复制到当前工作区，旧原件继续保留。旧聊天可按来源选择；已有资料不会被覆盖。
- 切换前保存正在编辑的内容。保存失败时保留草稿并提示重试，避免切换账号后丢失修改。
- 新用户的日历、待办、笔记和专注入口默认关闭；新手引导会介绍“账户”中的功能设置，个人与团队用户均可按需开启。已有用户的选择保留。
- 改善多设备同步时示例日程反复出现、已删除内容再次回来的情况；删除记录保护需要配套升级协作服务器。
- 资料读取失败时显示明确的重试入口，并保留原文件，避免把读取失败误当作空白资料。

### 团队连接

- 普通成员仍可查看管理员下发的代理连接信息，但不能修改这些字段；已获授权的代理方式仍可正常切换。
- 主页的代理方式选择与各 AI 的默认出口分别生效；管理员仍可为 ChatGPT、Claude、Gemini 指定不同线路，已有独立环境保留自己的线路绑定。
- 移除主页重复的机场稳定性提示，连接问题仍可通过代理检测和实际错误提示排查。

### 管理员体验

- 管理端新增 ChatGPT、Claude 和 Gemini 成功发送统计，可按日期查看次数与成员排行。
- 最新管理能力调整到导航前部；三种 AI 的默认出口与线路目录统一在“AI 线路策略”中维护，团队统一代理页面只保留连接参数和域名规则。
- 管理端安装包版本与 ShareGPT 客户端版本同步，便于确认正在使用的管理工具版本。

### 数据保护与团队升级

- 保存的登录及接口凭据使用本机安全存储保护；配置导出默认不包含密钥，需要时可明确选择导出。
- 加强团队权限检查与账号管理，改善同时编辑用户时的保存，并保护最后一个可用管理员账号。
- 协作服务加强用户库和个人同步资料的备份恢复，读取异常时保留原件。
- 升级无需先卸载。首次接续旧资料时，请先进入目标个人工作区或团队账号，再到“账户 → 本机资料”按类别确认；不要把其他账号的资料导入当前账号。
- 新协作服务继续兼容现有 1.0.x 客户端；服务器侧修复需要管理员单独升级后生效。桌面安装不会自动升级服务器。

## [1.0.9] - 2026-09-05

> **个人与团队双工作区、全新翻译工作台**。你可以在本机独立使用 ShareGPT，也可以连接团队，共享管理员提供的翻译与网络服务。本次还改善了聊天操作、账号切换和 AI 网页恢复体验。

### 选择适合你的使用方式

- 首次启动新增使用方式引导，可选择“连接团队”或“仅在本机使用”；之后不再重复欢迎引导。重启会回到登录入口，团队自动登录遵循已保存的设置；个人用户可从“返回选择使用方式”再次进入自己的工作区，原有数据继续保留。
- 个人工作区无需团队账号，可使用自己的代理和翻译接口；聊天、在线成员、团队线路及管理功能只在登录团队后显示。
- 可随时从“账户”切换个人与团队工作区，分别保留自己的代理、翻译设置和 ChatGPT、Claude、Gemini 登录状态及历史会话。
- 个人工作区也可按 AI 服务管理、清除或重建本地网页数据。

### 全新翻译工作台

- 翻译栏分为“阅读翻译”和“写给 AI”：前者用于阅读和复制，后者先生成可编辑预览，确认后插入 AI 输入框。
- 阅读翻译支持手动输入、读取网页选中文字或读取当前页面；写给 AI 可选择“仅译文”或“原文 + 译文”。
- 如果 AI 输入框里已经有草稿，插入前会让你选择取消、追加到末尾或替换原草稿，避免覆盖正在编辑的内容。
- 翻译进行中可以随时停止，未完成的结果会明确标记。
- 翻译栏可拖动调整宽度、双击恢复默认，并会在较窄窗口自动调整布局；所选宽度会按当前工作区保存。
- 支持自然、直译和简洁三种翻译风格、术语表、自动翻译网页选区，以及可选的发送语言提醒。发送提醒默认关闭，不会改变日常 Enter 操作。
- 支持自己的 OpenAI 兼容接口、通用翻译接口或本地离线服务；使用 HTTP 时会提示连接安全性。

### 团队翻译与管理员控制

- 管理员可在管理端创建多套团队翻译服务、设置默认选项，并决定向全部成员开放或只授权指定账号。
- 团队 API Key 由协作服务器加密保存，成员直接选择获授权的翻译服务。
- 管理端可按日期、用户和翻译配置查看成功请求数、输入字符、token 和估算费用。
- 未配置团队翻译或未获授权时，成员仍可继续使用自己的 API 或本地离线翻译，不影响其它功能。

### 团队网络与线路

- 团队首页的代理参数由管理员维护，成员直接使用下发配置；个人工作区仍可编辑自己的连接。
- 管理员更新团队网络配置后，客户端会提示变更、安全停止旧线路并同步新设置；同步完成后，请重新开启代理。
- 管理员可分别为 ChatGPT、Gemini 和 Claude 推荐默认线路，让不同 AI 服务使用不同出口。
- 获授权的高级用户可为多个 AI 环境选择可用线路。
- 网络诊断分别展示出口检测结果和配置状态，便于查看连接情况。

### 协作与消息

- 消息菜单、表情回应和长消息面板会根据窗口空间调整位置和大小，靠近聊天底部也方便操作。
- 切换会话后再回来，可继续编辑本次运行期间未发出的文字；不同账号、群聊和私聊的草稿互相分开。发送失败时保留输入，取消编辑消息也会恢复原来的草稿；退出应用后的草稿恢复暂不支持。
- 阅读旧消息时，新消息不会强行把页面拉回底部；可通过新消息提示回到底部。查看引用消息后可以返回原阅读位置，减少来回翻找。
- 聊天侧栏持续显示“在线人数 / 成员总数”。
- 应用内消息提示移到右上角，并新增“查看”操作；点击后会打开对应群聊或私聊，定位并短暂高亮相关消息。
- AI 网页使用量按成功发送的消息统计。
- 协作服务器新增聊天历史和发送统计的备份恢复保护，需升级服务器后使用。

### 账号、AI 网页与数据

- 加强不同服务器和账号的设置、翻译、Notes AI 与网页登录资料隔离。
- 改善快速切换标签、全屏切换、系统唤醒和网页异常后的恢复，以及 Claude 页面加载后的输入体验。
- 保存的 AI 环境和网页资料会在退出账号后保留，重新登录并获得相应权限后可继续使用。
- 改善 Notes AI 切换账号、环境和文档时的任务处理。

### 体验与升级修复

- 个人资料编辑采用更清晰的单栏布局，窄窗口也可完整操作；保存中会防止重复提交，保存失败保留已填内容，便于重试。
- 减少打开 AI 网页时的重复检查，切回已打开的页面更直接。
- 修复 macOS 进入全屏后标题栏位置错位，以及聊天输入框获得焦点时出现多余阴影或滚动条的问题。
- AI 网页标签较多时，横向滚动提示改为指针移入后显示，并支持拖动，不再长期占据工具栏空间。
- 个人代理设置简化为协议、地址和端口，其余内部端口自动分配。
- 改善工作区切换后的初始化，以及接收端模式下的代理运行。
- 改善更新版本识别和安装包匹配，升级继续沿用原有 ShareGPT 数据目录。
- 保持现有 1.0.x 客户端与协作服务器兼容，新客户端连接旧服务器时可继续使用其支持的功能。

### 升级前请留意

- 从 1.0.x 升级时，无需先卸载，原有数据继续保留。
- 团队共享翻译、管理员用量报表和服务器历史备份保护需要同步升级协作服务器。管理员请先备份服务器数据，并按[自托管指南](docs/SELF_HOSTING.md)配置翻译服务的加密密钥；只更新桌面客户端不会自动升级服务器。
- 首次安装可能出现系统安全提示：Windows 安装包未签名，Mac 版未经 Apple 公证，具体操作见 Release 安装说明。

## [1.0.8] - 2026-08-26

> **独立 AI 环境、多线路与翻译侧栏**：ChatGPT、Gemini 和 Claude 可以使用相互隔离的网页登录环境，并由管理员分配可用线路。AI 网页工作区同时加入翻译侧栏和更稳定的缩放、切换体验。

### 独立环境与线路

- 可为 ChatGPT、Gemini 和 Claude 创建多个独立环境；每个环境分别保存网页登录状态、网页数据、名称和线路绑定。
- 管理员可维护多条内置线路、检查预期出口，并按用户开放高级 AI 环境及可用线路；普通 AI 使用方式保持不变。
- 新建环境时可直接选择已授权线路。线路不可用、出口检查失败或网页没有绑定到指定线路时会停止打开，不会偷偷换到其它出口。

### 翻译与网页工具

- 高级 AI 环境加入翻译侧栏，可手动输入、读取当前网页或翻译选中文字；支持常用语言自动检测和互译。
- 翻译可使用 AI 兼容接口、LibreTranslate 兼容接口或本机离线服务，并可一键复制结果。
- Claude 工具栏新增按需网页入口，可在新的内部标签页打开 HTTP/HTTPS 页面，并使用前进、后退、刷新和多标签操作。

### 体验修复

- AI 顶部信息栏可以收起；按 `Esc` 可快速恢复界面区域。
- 修复快捷键或滚轮缩放后外层界面与网页比例不同步、网页边界偏移和工具栏被遮挡的问题。
- 修复切换 ChatGPT、Gemini、Claude 时旧页面残留或不必要刷新，以及 Claude 新标签偶发加载中断的问题。

## [1.0.7] - 2026-08-20

> **运行时与跨平台兼容更新**：升级 Electron/Chromium，修复 Mac 继承旧版 Windows 环境预设后的平台矛盾，并加强双平台正式包校验。建议升级。

### 变更

- Electron 固定升级到 `43.1.0`（Chromium 150）；开发和打包环境最低要求提升到 Node.js `22.12.0`。
- macOS/Linux 收到旧版同步的 `us-windows` 预设时自动回落到均衡预设；Windows 继续兼容已有配置。
- Mac 设置页不再提供 Windows 环境预设，并始终保留真实 macOS 平台、UA、架构、字体和图形信息；国家、语言与时区仍可独立配置。

### 发布与安全

- Windows 正式发布校验扩展到 NSIS 身份、`latest.yml`、`.blockmap`、渲染入口、Electron 版本以及 sing-box/frpc 固定校验值。
- 构建配置明确排除渲染层本机 `.npm-cache`，发布检查会在缓存意外进入 ASAR 时直接失败。
- 更新 `ip-address`、`js-yaml` 与 `ws` 的安全修复版本，消除当前生产依赖审计中的高危告警。

### 验证

- 新增跨平台配置回归测试，覆盖非 Windows 主机降级旧预设、Mac 隐藏 Windows 选项及原生平台信息保留。
- Electron 隐私清理、UI 流程和指纹标准化继续通过真实 Chromium 集成测试；正式产物按同一发布提交分别在 Apple Silicon Mac 与 Windows x64 构建。

## [1.0.6] - 2026-07-10

> **网页可见信息表盘更新**：把 AI 网页实际看到的网络、浏览器、硬件与图形摘要集中展示，并加入可选的本机资料重建和稳定标准化。建议升级。

### 新增

- **网页可见信息表盘**：分别采集 ChatGPT、Claude、Gemini 当前页面实际可见的出口 IP、ASN、国家、时区、语言、WebRTC、UA、Client Hints、CPU、内存、屏幕、触控、WebGL/GPU、Canvas、Audio、字体和媒体设备摘要。
- **环境矛盾提示**：直接标出出口时区与网页时区不一致、会话未确认使用 SOCKS、WebRTC 防泄漏策略未生效、本地 IP 候选和 `navigator.webdriver` 等问题。
- **清除前后对比**：清除网页登录数据或重建资料环境前自动保留摘要；重新打开网页并刷新后，可逐项查看 19 个字段的变化。
- **Mac / Windows 对端对比**：快照可复制为 JSON，并在另一台设备手动导入，比较同一同步配置下仍存在的系统、硬件和图形差异；快照不会自动上传。
- **按服务重建资料环境**：ChatGPT、Claude、Gemini 可分别清理旧分区并切换到新的本机持久化分区，避免旧站点存储继续沿用；操作仍需当前协作账号密码复核。
- **可选稳定标准化**：默认关闭；开启后可使用均衡预设或美国 Windows 预设，稳定 CPU/内存档位、屏幕/DPR、触控、Canvas/Audio 摘要等网页可见值。

### 改进

- ChatGPT、Claude、Gemini 的侧栏与网页工作区改用各自品牌轮廓图标，深浅色主题下保持一致可读。

### 隐私与兼容

- 采集只保存摘要和哈希，不保存 Canvas 原图、音频样本或媒体设备 ID；本机资料 ID、网页审计快照和清除记录不参与跨设备同步。
- 同步数据继续使用兼容 1.0.5 及更早客户端的现有载荷版本；旧客户端会忽略新增字段，服务器无需新增接口。
- 标准化不能改变真实出口 IP，也不承诺隐藏代理、绕过网站风控或让不同物理设备绝对不可区分；表盘用于发现并逐项减少明显矛盾。

### 验证

- 新增主进程单元测试，覆盖设置归一化、Windows UA/Client Hints、稳定注入、资料分区轮换和摘要生成。
- 新增真实 Electron/Chromium 指纹测试，验证标准化后的平台、CPU、内存、屏幕、媒体设备及 Canvas/Audio 摘要。

## [1.0.5] - 2026-07-10

> **隐私与环境更新**：新增按服务清理网页登录数据、浏览器环境配置、代理出口同步与跨设备配置同步。建议升级。

### 新增

- **按服务清理网页登录数据**：可分别清除 ChatGPT、Gemini 或 Claude 的 Cookie、LocalStorage、IndexedDB、Cache Storage、Service Worker、HTTP 认证及网络/代码/DNS 缓存；不提供“一键清除全部”。
- **密码二次确认**：清除前必须重新输入当前协作账号密码；服务器只复核密码、不签发新会话，也不会挤掉当前登录。
- **浏览器环境配置**：支持跟随本机、美国预设或跟随代理出口；美国/代理模式使用 `en-US`，时区可选择美国时区或从出口自动校准。
- **出口位置同步**：两条独立链路返回同一出口 IP 后才采用检测结果；地理位置可完全关闭，或只返回与出口 IP 一致的城市级粗略位置。
- **跨设备同步**：同步非敏感的语言、美国预设时区与位置策略；每台设备单独检测当前代理节点，不上传 Cookie、密码、网页登录态、出口 IP、位置检测结果或本机清理记录。

### 修复

- 修复清除 ChatGPT / Gemini / Claude 网页数据后首次打开可能停留在 `environment bootstrap` 空白页的问题；现在会自动重建标签、保持初始化遮罩，并对瞬时网络超时进行重试。
- GPT、Gemini、Claude 页面加载错误条新增可点击的关闭按钮；页面恢复加载后也会自动清除旧错误。

### 隐私

- AI 网页启用 WebRTC `disable_non_proxied_udp` 策略，避免非代理 UDP 暴露真实网络地址。
- 未启用出口地理位置时，网页的地理位置权限会被直接拒绝，不回落读取系统真实位置。
- 本功能用于减少隐私泄漏和环境矛盾，不承诺删除服务商服务器记录或让网站无法识别代理。

### 验证

- 新增只访问 `127.0.0.1` 的 Electron/Chromium 自测，验证语言、时区、地理位置、WebRTC 策略和分区清理，不访问任何 AI 网站。
- macOS 已覆盖 Cookie、LocalStorage、IndexedDB、Cache Storage、Service Worker 清理，以及其它 AI 分区不受影响的隔离测试。

## [1.0.4] - 2026-06-24

> **功能更新**：安装可自选位置（可装到 C 盘以外）；消息通知默认更安静；表情组合更快。建议升级。

### 新增

- **安装可选位置**：Windows 安装器改为向导式，安装时可**选择安装目录**（C 盘空间不足可装到 D/E 盘）。免管理员；卸载保留数据。
- **协作消息表情组合更快**：内置 Emoji Kitchen 精简索引（本地判定有无组合），**直连 Google 取图、去掉第三方代理**——无组合立即显示两个表情、有组合无冷启动且取过即缓存。

### 变更

- **消息弹窗 / 提示音默认关闭**：避免新用户被打扰；可在「账户 → 协作通知」按需打开。系统通知仍默认开。
- **新手引导补全**：新增「笔记 / 知识库」「专注 / 番茄钟」介绍，并在账户步骤说明通知开关；聊天/AI 步骤补充表情与缩放提示。
- 笔记列表、命令面板等列表类区域**隐藏滚动条**，更清爽。

### 修复

- **被管理员禁用的账号**不再出现在会话列表中（服务端本就阻止其登录并排除出成员目录，此处补齐历史会话残留）。

### 备注

- **从旧版搬到其它盘**：自动更新仍为原地静默更新、不会迁移位置；若 C 盘不足想换盘，**先卸载再用新安装包选择新盘重装**即可，账号 / 聊天 / 笔记等数据存于 `%APPDATA%\ShareGPT`（与安装目录无关），重装不丢。
- macOS 仍为手动下载 dmg、无原地自动更新。

## [1.0.3] - 2026-06-24

> **功能更新**：新增「笔记 / 知识库」与「专注 / 番茄钟」两大模块，并为协作聊天加上表情回应。建议升级体验新功能。

### 新增

- **笔记 / 知识库（Obsidian 式）**：本地优先的真 `.md` 仓库（可直接用 Obsidian 打开）；双链 `[[..]]` 与反链、Live Preview 编辑（CodeMirror 6）、全文检索、标签嵌套树、可编辑属性（frontmatter）。
- **关系图谱**：全局 / 局部视图，悬停高亮邻居，**点击节点平滑居中放大**；并支持**全库 AI 自动连线**（让 AI 分析全库、建议并批量建立双链）。
- **AI 辅助写作**：接入 Codex（OpenAI Responses API，流式）——扩写 / 续写 / 总结 / 润色 / 改写 / 起标题 / 翻译 / 标签建议 / RAG 问答；选区或 ⌘K 内联改写，带 diff 评审（保留 / 放弃 / 重试 / 撤回）。
- **Canvas 白板 + Bases 数据库视图**：JSON Canvas 白板、笔记的表格 / 看板式视图；命令面板、快速跳转、日记。
- **笔记云端同步**：整库 blob + 三方合并的单会话顺序模型（写入云端、另一端打开时与本地对比合并）。
- **专注 / 番茄钟**：计时器（专注 / 短休 / 长休）、任务绑定、白噪音（白 / 棕 / 雨声）、专注统计与连续天数；**团队专注排名**（今日 / 本周）。
- **协作消息表情**：输入框新增表情选择器，可在消息里**插入 emoji 发送**；消息表情**回应**（Telegram 式全 emoji，长按 / 悬停拾取、聚合显示）。
- **Telegram 式表情放大**：整条只含 1～3 个 emoji 时自动**放大并动态播放**（Google Noto 动态 emoji，无动画则回退静态）；恰好两个 emoji 时尝试 **Emoji Kitchen 组合贴纸**（无组合则并排放大）。
- **可隐藏内容入口** + **左侧导航长按拖动排序**（顺序跨设备记忆）。

### 修复

- 各群协作聊天严格隔离，不再串台；Electron 下用应用内输入弹窗替换不可用的 `window.prompt`。
- AI 接口遇上游过载 / 限流 / 超时自动退避重试并友好提示；Canvas 夜间配色修正。
- 修复打开消息表情回应面板时把整页撑出**横向滚动**（面板改为朝内侧展开 + 消息区裁剪横向溢出）。

### 备注

- 笔记云同步、团队专注排名、消息表情回应依赖协作服务端对应能力（已部署的服务器自动生效，否则相应功能自动降级 / 仅本地）。
- macOS 为「提示下载安装包」（dmg）方式，无原地自动更新。

## [1.0.2] - 2026-06-23

> **可选更新**：新增日历 / 待办 等效率模块；旧版本仍可正常使用，非必需升级。

### 新增

- **个人日历**：月 / 周 / 日视图，事件增删改、多日历（颜色 / 显隐）、当前时间线、重复事件；支持**导入外部日历（.ics）**。
- **组队日历**：团队共享事件、邀请成员与 接受 / 拒绝 / 待定（RSVP）、成员筛选；经协作服务器同步，未登录可本地预览。
- **备忘录 / 待办**：清单 + 智能视图（今天 / 最近 7 天 / 收件箱 / 全部 / 已完成）、优先级 / 标签 / 子任务 / 重复；**自然语言快速添加**（中文连写如「明天下午5点写周报 !high #工作」，基于 chrono-node）；便签看板（置顶 / 配色）。
- **跨功能打通**：待办可「加入日历 / 一键同步到日历」，个人事件可「共享到团队」；新手引导覆盖新模块。
- **个人日历 / 待办云端同步**：按用户多端同步、WebSocket 实时、版本号乐观并发**防止旧版本覆盖新版本**（服务端 `/api/user-store`；需部署服务端后生效，未支持时自动「仅本地」）。

### 修复

- 登录后偶发**「账号在别处登录」**：云同步/组队日历不再自建第二条 WebSocket，全应用复用聊天的唯一连接。
- **协作聊天历史 / 成员按「群」隔离**：同一客户端连不同协作服务器不再串台，登录即切换。
- 快速添加**中文连写「分词」**：改用 chrono-node，连写也能正确识别日期/时间并剥离出干净标题。

### 优化

- 侧栏导航过多时可滚动（隐藏滚动条），收起按钮与版本卡固定底部；三大模块整体字体放大、便签 UI 重做（参考 Keep，任意底色文字可读）。

## [1.0.1] - 2026-06-22

> **可选更新**：本版以体验优化与小问题修复为主，旧版本仍可正常使用，非必需升级。

### 新增

- **未登录预览**：登录页新增「先不登录，随便逛逛」，可进入只读主界面预览；顶部预览条可随时返回登录。
- **新手引导**：首次进入主界面自动弹出分步高亮导览，逐一介绍核心面板；标题栏「?」可随时重看。
- **内置网页右键菜单**：内嵌 AI 网页支持浏览器式右键菜单（后退 / 前进 / 重新加载、剪切 / 复制 / 粘贴、复制链接、在浏览器打开、复制图片、检查元素等）。
- **登录页改版**：加入品牌欢迎区与功能简介，开局更友好。

### 修复

- **代理重启端口冲突**：修复重启 sing-box 时偶发的端口占用（`bind: only one usage...` / EADDRINUSE）导致代理起不来、反复重启失败；现等旧进程退出、端口释放后再启动，并避免旧进程退出时误清新进程引用。
- **「一键加入并重启」不生效**：修复默认（all）模式下自动加入的域名（`auto_domains`）未并入实际代理路由、且代理检测始终判为「回落」的问题；现路由规则与代理检测共用同一份域名清单，加入并重启后即可正确走代理并翻绿。

### 备注

- 本版不强制更新。AI 网页内的翻译工具与共享协作空间已在规划中（见 `docs/`）。

## [1.0.0] - 2026-06-19

首个正式版本。

### 新增

- 整合 **ChatGPT / Claude / Gemini** 三家 AI 网页于一个客户端（多标签、各自独立会话、登录态持久化，入口可在设置中开关）。
- **统一代理**：基于 sing-box，按内置域名清单仅对 AI 站点走代理；管理员统一下发连接配置，成员首登自动拉取，无需手配。
- **代理检测**：实时显示页面流量是否全部走代理；发现「会用到却没走代理」的域名时自动加入本机清单并上报管理员，重启即时生效。
- **可选机场订阅模式**：管理员粘贴 Clash 订阅、选节点下发，客户端可选择走机场节点（与统一代理并存，默认统一）。
- **团队协作聊天**：私聊 / 房间消息、图片与文件、撤回 / 已读 / 回复 / 转发、离线补同步、可自定义提醒。
- **使用统计**：按 ChatGPT / Gemini / Claude 维度统计每人查询量与排行。
- **管理控制台**：用户管理、Sender 默认配置下发、机场节点下发、用户反馈查看、漏走代理域名汇总、版本发布。

### 变更

- **自动更新改为以 GitHub Releases 为更新源**（参考 [cc-switch](https://github.com/farion1231/cc-switch)），不再经过任何自建服务器。
- **Windows 原地无感更新**：后台下载、自动安装并重启，快捷方式与安装位置不变，账号 / 聊天记录 / 网页登录态全部保留；macOS 暂为下载安装包方式。

### 备注

- 更早的 5.x 为测试版本，不在此正式记录。

[Unreleased]: https://github.com/Sjeary/ShareGPT/compare/v1.0.10...HEAD
[1.0.10]: https://github.com/Sjeary/ShareGPT/compare/v1.0.9...v1.0.10
[1.0.9]: https://github.com/Sjeary/ShareGPT/compare/v1.0.8...v1.0.9
[1.0.8]: https://github.com/Sjeary/ShareGPT/compare/v1.0.7...v1.0.8
[1.0.7]: https://github.com/Sjeary/ShareGPT/compare/v1.0.6...v1.0.7
[1.0.6]: https://github.com/Sjeary/ShareGPT/compare/v1.0.5...v1.0.6
[1.0.5]: https://github.com/Sjeary/ShareGPT/compare/v1.0.4...v1.0.5
[1.0.4]: https://github.com/Sjeary/ShareGPT/compare/v1.0.3...v1.0.4
[1.0.3]: https://github.com/Sjeary/ShareGPT/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/Sjeary/ShareGPT/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/Sjeary/ShareGPT/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Sjeary/ShareGPT/releases/tag/v1.0.0
