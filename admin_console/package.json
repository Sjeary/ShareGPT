{
  "name": "sharegpt-admin-console",
  "version": "1.0.10",
  "description": "Standalone admin console for ShareGPT server management.",
  "main": "src/main/main.js",
  "author": "Sjeary",
  "license": "UNLICENSED",
  "scripts": {
    "build:ui": "npm --prefix ui ci && npm --prefix ui run build",
    "dev": "npm run build:ui && npm exec -- electron .",
    "dist:win": "npm run build:ui && npm exec -- electron-builder --win portable",
    "dist:mac": "npm run build:ui && npm exec -- electron-builder --mac dmg"
  },
  "devDependencies": {
    "electron": "43.1.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "com.sjeary.sharegpt.admin",
    "productName": "ShareGPT Admin",
    "asar": true,
    "files": [
      "src/**/*",
      "ui/dist/**/*",
      "package.json",
      "!ui/src/**",
      "!ui/node_modules/**",
      "!ui/package*.json",
      "!ui/tsconfig*.json",
      "!ui/vite.config.ts",
      "!ui/components.json"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": [
        "portable"
      ],
      "artifactName": "sharegpt-admin-${version}.${ext}",
      "executableName": "sharegpt_admin",
      "signAndEditExecutable": false
    },
    "mac": {
      "target": [
        "dmg"
      ]
    }
  }
}
